const connection = require('./connection')

function selectionALL () {
  return new Promise((resolve, reject) => {
    let query
  })
}

function insertOne () {
  return new Promise((resolve, reject) => {
    let query
  })
}

function updateOne () {
  return new Promise((resolve, reject) => {
    let query
  })
}
